import LoginBase from './page/base'

describe('Login Route Unit Tests', () => {
  let LoginBase;

  beforeEach(() => {
    login = new LoginBase()
  })

  test('Should Allow Input To Be Entered', () => {
  });

  test('Should Redirect to Forgot Password Page on Link Click', () => {
  });

  test('Should Display Error Message on Invalid Email', () => {
  });

  test('Should Display Error Message on Invalid Login', () => {
  });

});
