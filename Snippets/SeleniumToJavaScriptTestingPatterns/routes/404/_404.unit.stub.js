import PageBase from './page/base'

describe('404 Unit Tests', () => {
  let PageBase;

  beforeEach(() => {
    login = new PageBase()
  })

  test('Back to homepage has correct link', () => {
  });

});
