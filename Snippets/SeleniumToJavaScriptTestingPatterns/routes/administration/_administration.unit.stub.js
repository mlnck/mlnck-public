import PageBase from './page/base'

describe('Administration Users Unit Tests', () => {
  let PageBase;

  beforeEach(() => {
    login = new PageBase()
  })

  test('Only One Tab Active at a Time', () => {
  });

  test('Applicable Tabs show Current Count in Title', () => {
  });

  test('Add New Button Functionality Toggles When Edit Clicked', () => {
  });

  test('Toggle Buttons Functionality Toggle When Edit Clicked', () => {
  });

  test('Input Fields Functionality Toggle When Edit Clicked', () => {
  });

  test('Lock and Edit Button Toggle When Clicked', () => {
  });

  describe('After Edit Button Clicked', () => {

    describe('Focused on Users Tab', () => {

      test('"Save User" Button is Enabled When Content Changed', () => {
      });

    });

    describe('Focused on Organization Tab', () => {

      test('Display Save Message when Unsaved Changes are Present', () => {
      });

    });
  })

});
