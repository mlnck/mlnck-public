import PageBase from './page/base'

describe('Add A Rider Unit Tests', () => {
  let PageBase;

  beforeEach(() => {
    login = new PageBase()
  })

  test('Should Display Approval When Required Field Criteria Met', () => {
    //red asterisk to green checkmark (couldn't think of how else to explain that)
  });

  test('Should Validate Phone Fields', () => {
  });

  test('Should Validate Email Fields', () => {
  });

  test('Should Pre-Populate Billing Field', () => {
  });

  test('Should Allow Only One Radio Label to be Active at a time', () => {
  });

  test('Should Update Section Number to Check Mark When Section is Complete', () => {
  });

  test('Should Allow You to Skip Non-Required Section on "Skip this Section" Click', () => {
  });

  test('Should Allow You to Add Optional Care Contact', () => {
    //handle confirmation toggle in here as well
  });

  test('Should Allow You to Add Rider Note(s)', () => {
  });

  test('Should Allow You to Upload File of Correct Type', () => {
  });

});
