# A Skeleton Boilerplate for a Mongo Express & Node App

No React, no angular.  
Just the backend setup with a single route and view.

## Usage:
* Download/Clone
* cd into folder
* `mv conf.env .env`
* `npm install`
* `npm start`
